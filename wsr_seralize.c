
#include "wsr_task.h"


WSR_BUFFER_LIST *get_dependent_buffers(WSR_TASK_LIST_P  task_list, int num_tasks) {
    
}

//Serialise list of tasks and the accessed buffers
//Return the single serilzed buffer and its size
//Memory for the buffer is allocated inside the function
//
int wsr_seralize_tasks(WSR_TASK_LIST *task_list, int num_tasks){

    get_dependent_buffer(taskt_list, num_tasks);

}


